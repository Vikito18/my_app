import React from "react";
import Button from "react-bootstrap/button";

const ButtonReturn = () => {
  return(
    <Button variant="danger" type="button" className="btn btn-secondary" size="lg">Retour</Button>
  );
};

export default ButtonReturn;